═══════════════════════════════════════════════════════════════════════════════
RETELL AGENT INSTRUCTION FLOW
═══════════════════════════════════════════════════════════════════════════════

┌─────────────────────────────────────────────────────────────────────────────┐
│ 1. USER INITIATES CALL                                                      │
│    Frontend → POST /api/retell/create-web-call                              │
│    └─ Loads: api_credentials (Retell API key, agent ID)                     │
└─────────────────────────────────────────────────────────────────────────────┘
                                    ↓
┌─────────────────────────────────────────────────────────────────────────────┐
│ 2. RETELL CONNECTS TO WEBSOCKET                                             │
│    wss://your-domain/llm-websocket/:org_slug/:call_id                       │
│                                                                              │
│    Loads Channel Config:                                                    │
│    ┌───────────────────────────────────────────────────────────────────┐   │
│    │ channel_configurations (per-org)                                  │   │
│    │   • enabled: true/false                                           │   │
│    │   • ai_backend: 'openai_gpt4o'                                    │   │
│    │   • data_integrations: ['opendental']                             │   │
│    │   • instructions: null or "custom instructions"  ← OPTIONAL       │   │
│    └───────────────────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────────────────┘
                                    ↓
┌─────────────────────────────────────────────────────────────────────────────┐
│ 3. USER SPEAKS → GREETING AGENT PROCESSES                                   │
│    src/app/agentConfigs/embeddedBooking/greetingAgentSTT.ts                 │
│                                                                              │
│    Instruction Build Process:                                               │
│    ┌───────────────────────────────────────────────────────────────────┐   │
│    │ PRIORITY 1: Channel Override                                      │   │
│    │   • channel_configurations.instructions                           │   │
│    │   • Status: Usually NULL (not commonly used)                      │   │
│    └───────────────────────────────────────────────────────────────────┘   │
│                              ↓ if null                                       │
│    ┌───────────────────────────────────────────────────────────────────┐   │
│    │ PRIORITY 2: Database Persona                                      │   │
│    │   • domains.system_prompt_template                                │   │
│    │   • Template variables: {persona_name}, {company_name}            │   │
│    │   • Status: ✅ LOADED if configured                               │   │
│    │   • Cache: Loaded once on server startup                          │   │
│    └───────────────────────────────────────────────────────────────────┘   │
│                              ↓ if not loaded                                 │
│    ┌───────────────────────────────────────────────────────────────────┐   │
│    │ PRIORITY 3: Hardcoded Fallback                                    │   │
│    │   • greetingAgentSTT.ts lines 80-171                              │   │
│    │   • Status: ⚠️ FALLBACK ONLY                                      │   │
│    │   • Contains: 500+ lines of booking instructions                  │   │
│    └───────────────────────────────────────────────────────────────────┘   │
│                                                                              │
│    ALWAYS APPENDED:                                                          │
│    ┌───────────────────────────────────────────────────────────────────┐   │
│    │ • Office Info (dentalOfficeData.ts) - HARDCODED                   │   │
│    │ • Tool Definitions (get_datetime, get_office_context, etc.)       │   │
│    └───────────────────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────────────────┘
                                    ↓
                     User asks to book/reschedule?
                                    ↓
┌─────────────────────────────────────────────────────────────────────────────┐
│ 4. HAND OFF TO ORCHESTRATOR                                                 │
│    src/app/agentConfigs/embeddedBooking/orchestratorAgent.ts                │
│                                                                              │
│    Instruction Build Process:                                               │
│    ┌───────────────────────────────────────────────────────────────────┐   │
│    │ PRIORITY 1: Database Workflows + Rules                            │   │
│    │   • agent_workflows table (booking flows)                         │   │
│    │     - Columns: name, description, steps[]                         │   │
│    │   • business_rules table (critical constraints)                   │   │
│    │     - Columns: title, rule_text, severity, applies_to[]          │   │
│    │   • Cache: 60 seconds TTL                                         │   │
│    │   • Status: ✅ LOADED if configured                               │   │
│    └───────────────────────────────────────────────────────────────────┘   │
│                              ↓ if database fails                             │
│    ┌───────────────────────────────────────────────────────────────────┐   │
│    │ PRIORITY 2: Hardcoded Workflows                                   │   │
│    │   • orchestratorAgent.ts lines 245-681                            │   │
│    │   • Status: ⚠️ FALLBACK ONLY                                      │   │
│    │   • Contains: 8 complete booking workflows                        │   │
│    └───────────────────────────────────────────────────────────────────┘   │
│                                                                              │
│    ALWAYS APPENDED:                                                          │
│    ┌───────────────────────────────────────────────────────────────────┐   │
│    │ • Function Catalog (49 OpenDental API functions)                  │   │
│    │   - From: apiRegistry.ts (PRIORITY_FUNCTIONS list)                │   │
│    │ • Current Date Info (today, tomorrow, next week)                  │   │
│    │ • Parameter Quick Reference (API function signatures)             │   │
│    └───────────────────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────────────────┘
                                    ↓
┌─────────────────────────────────────────────────────────────────────────────┐
│ 5. ORCHESTRATOR CALLS BOOKING API                                           │
│    src/app/api/booking/route.ts                                             │
│    └─ Executes: GetPatients, GetAvailableSlots, CreateAppointment, etc.     │
└─────────────────────────────────────────────────────────────────────────────┘
                                    ↓
┌─────────────────────────────────────────────────────────────────────────────┐
│ 6. RESPONSE FLOWS BACK TO USER                                              │
│    Orchestrator → Greeting Agent → WebSocket → Retell → User                │
└─────────────────────────────────────────────────────────────────────────────┘

═══════════════════════════════════════════════════════════════════════════════
INSTRUCTION SOURCES SUMMARY
═══════════════════════════════════════════════════════════════════════════════

DATABASE (✅ Configurable):
  • domains.system_prompt_template      ← Greeting agent persona
  • agent_workflows                     ← Booking/reschedule flows
  • business_rules                      ← Critical constraints
  • channel_configurations.instructions ← Channel-specific override (optional)

HARDCODED (🔴 Static):
  • dentalOfficeData.ts                 ← Office name, phone, hours, services
  • apiRegistry.ts                      ← 49 OpenDental API functions
  • Tool definitions                    ← get_datetime, get_office_context, etc.
  • Fallback instructions               ← Used only when database fails

═══════════════════════════════════════════════════════════════════════════════
TESTING WHICH SOURCE IS ACTIVE
═══════════════════════════════════════════════════════════════════════════════

Look for these logs when a call starts:

  ✅ DATABASE ACTIVE:
     [Lexi] ✅ Persona prompt loaded from database
     [Lexi] Using database-configured persona
     [Orchestrator] Loaded 5 workflows and 8 business rules from database

  ⚠️ FALLBACK ACTIVE:
     [Lexi] Using hardcoded fallback persona
     [Orchestrator] Using hardcoded fallback configuration

═══════════════════════════════════════════════════════════════════════════════
HOW TO CUSTOMIZE RETELL INSTRUCTIONS
═══════════════════════════════════════════════════════════════════════════════

1. CHANNEL-SPECIFIC OVERRIDE (highest priority):
   
   UPDATE channel_configurations
   SET instructions = 'Custom instructions for Retell only'
   WHERE organization_id = 'your-org-id' AND channel = 'retell';

2. ORGANIZATION-WIDE PERSONA:
   
   UPDATE domains
   SET system_prompt_template = 'You are {persona_name}...'
   WHERE organization_id = 'your-org-id';

3. WORKFLOW CUSTOMIZATION:
   
   INSERT INTO agent_workflows (workflow_id, name, description, steps)
   VALUES ('custom', 'Custom Flow', 'Description', 
           '[{"text": "Step 1", "isMandatory": true}]'::jsonb);

4. BUSINESS RULES:
   
   INSERT INTO business_rules (title, rule_text, severity, applies_to)
   VALUES ('Never Double-Book', 'Check conflicts first', 'critical', 
           ARRAY['orchestrator']);

═══════════════════════════════════════════════════════════════════════════════
KEY TAKEAWAYS
═══════════════════════════════════════════════════════════════════════════════

✅ Greeting Agent: TRIES DATABASE FIRST, falls back to hardcoded
✅ Orchestrator: TRIES DATABASE FIRST, falls back to hardcoded
✅ Channel Config: Can override per-channel (usually null)
⚠️ Office Info: HARDCODED (but injected into database templates)
⚠️ API Functions: HARDCODED (required for function calling)

The system is MOSTLY DATABASE-DRIVEN with appropriate fallbacks!
